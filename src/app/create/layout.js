import React from "react";

const CreateProjectLayout = ({ children }) => {
  return (
    <div className="lg:px-4 mt-16 lg:mt-[4.6rem] max-w-6xl mx-auto relative">
      {children}
    </div>
  );
};

export default CreateProjectLayout;
